const validator = require("email-validator");

// User Model for validating email and password
export class User {
  email: string;
  password: string;
  // isValidEmail() {
  //  // return validator.validate(this.email);
  // }
}